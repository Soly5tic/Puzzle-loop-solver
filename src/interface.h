#ifndef INTERFACE_H
#define INTERFACE_H

#include <bits/stdc++.h>
using namespace std;

#include "solver.h"

void interfaceInit();
void Print(int dep, int &mxdep, int &dfscnt, int n, int m, int **a, int (*)[55], int (*)[55], int _TIME_);

#endif